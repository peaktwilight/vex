[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "vex-ai-runtime"
version = "0.1.0"
description = "Rust-backed secure inference runtime exploration for Python AI apps"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = []

[tool.maturin]
python-source = "python"
module-name = "vex_ai_runtime._core"
